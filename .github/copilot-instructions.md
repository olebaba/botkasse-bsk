You are working on a Next.js project using the app router. The project is set up with TypeScript and includes Tailwind CSS for styling.
It uses bun for package management and runtime.
The code is written in norwegian bokmål, so always keep the variable names and comments in norwegian bokmål.
The code should always be efficient and follow best practices for Next.js, TypeScript, and Tailwind CSS. 
React components should use the const with arrow function syntax.
Keep comments to a minimum, only where necessary for clarity. The code itself should be self-explanatory.
Remember to always use imports from the /lib directory for any utility functions or constants, especially the dayjs file.